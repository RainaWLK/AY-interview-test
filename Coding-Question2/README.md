# AWS EKS and k8s service sample

> Note: these are prototype only because it didn't be tested.

## Steps
1. run terraform
```
terraform init
terraform plan
terraform apply
```
2. install additional packages into k8s, then deploy apps
```
cd k8s
. init.sh
```
